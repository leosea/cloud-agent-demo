# Cloud Agent 演示任务（复制这段去启动）

请完成以下最小改动并开 PR：

1. 新建文件 `CLOUD_AGENT_DEMO.md`
2. 用中文写三行：
   - 本文件由 Cloud Agent 自动创建
   - 创建时间（当前日期）
   - 一句话：Cloud Agent 在独立云端 VM、独立分支上工作
3. 运行 `python hello.py` 确认输出包含 `Hello from`
4. 提交到分支 `cloud-agent-demo`，并创建 Pull Request

完成后回复：分支名、PR 链接、改动摘要。
